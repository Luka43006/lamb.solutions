# SMTP credentials for the contact form (src/app/api/contact/route.ts).
# Defaults below assume Namecheap Private Email; override if you switch providers.

SMTP_HOST=mail.privateemail.com
SMTP_PORT=587
SMTP_USER=info@lamb.solutions
SMTP_PASS=
CONTACT_TO_EMAIL=info@lamb.solutions
